## Assignment 3: WordPress Child Theme

### Child Themes

A child theme is to override existing functionality without modifying changes to the theme. The child theme prevents the original file from errors that affects results. A child theme is significant if there are additional changes needed in the script and you are not wanting the theme structure to result null.

### Template Hierarchy

Template Hierarchy is how wordpress ensures the file is running. Wordpress follows a precedent order that follows structure. The file ensures that each file performs its function and that the theme can run effectively. 

### Real-World Reflection

I changed the single post layout to add a publish time so readers can be aware of when the article is. This ensures credibility and awareness in modern topics. 
